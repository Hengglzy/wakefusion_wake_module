# WakeFusion 客户端使用手册

## 目录

1. [环境搭建](#一环境搭建)
2. [启动流程](#二启动流程)
3. [后端模块说明](#三后端模块说明)
4. [通信机制](#四通信机制)
5. [核心机制](#五核心机制)
6. [配置说明](#六配置说明)
7. [故障排查](#七故障排查)

---

## 一、环境搭建

### 1.1 前置条件

#### 系统要求
- **操作系统**：Windows 10/11（64位）
- **Python版本**：3.10 或 3.11（推荐 3.10）
- **Conda**：Anaconda 或 Miniconda（已安装并配置环境变量）
- **硬件设备**：
  - 麦克风：XVF3800（推荐）或系统默认音频输入设备
  - 摄像头：Orbbec Femto Bolt（推荐）或 USB 摄像头

#### 验证 Conda 安装
```powershell
# 打开 Anaconda PowerShell Prompt，验证 conda 是否可用
conda --version
# 应输出：conda 23.x.x 或更高版本

# 验证 Python 版本
python --version
# 应输出：Python 3.10.x 或 3.11.x
```

### 1.2 创建 Conda 环境

系统需要**两个独立的 conda 环境**，因为它们的 Python 依赖不同，混用会导致依赖冲突。

#### 步骤1：创建主环境 `wakefusion`

```powershell
# 在项目根目录下执行
cd D:\tools\cursor_project\wakefusion_wake_module

# 创建环境（Python 3.10）
conda create -n wakefusion python=3.10 -y

# 激活环境
conda activate wakefusion

# 验证环境
python --version
# 应输出：Python 3.10.x
```

#### 步骤2：安装主环境依赖

```powershell
# 确保在 wakefusion 环境中
conda activate wakefusion

# 安装项目依赖（从 requirements.txt）
pip install -r requirements.txt
```

**安装说明**：
- **预计耗时**：10-20 分钟（取决于网络速度）
- **关键依赖**：
  - `torch>=2.0.0`（CPU版，用于 Silero VAD 和 NeMo）
  - `torchaudio>=2.0.0`（CPU版）
  - `nemo-toolkit[asr]>=1.14.0`（NVIDIA NeMo 框架，包含 MatchboxNet）
  - `pyzmq>=25.0.0`（ZMQ 通信）
  - `websockets>=11.0`（WebSocket 通信）
  - `pyyaml>=6.0`（配置文件解析）
  - `pydantic>=2.0`（数据验证）
  - `colorlog>=6.7.0`（彩色日志）

**常见问题**：
- **PyTorch 安装慢**：使用国内镜像源
  ```powershell
  pip install torch torchaudio -i https://pypi.tuna.tsinghua.edu.cn/simple
  ```
- **NeMo 安装失败**：先安装 PyTorch，再安装 NeMo
  ```powershell
  pip install torch torchaudio
  pip install nemo-toolkit[asr]
  ```
- **pyaudio 安装失败**（Windows）：
  ```powershell
  # 方法1：使用预编译 wheel
  pip install pipwin
  pipwin install pyaudio
  
  # 方法2：从 conda-forge 安装
  conda install -c conda-forge pyaudio
  ```

#### 步骤3：创建视觉环境 `wakefusion_vision`

```powershell
# 退出当前环境（重要！）
conda deactivate

# 创建视觉环境（Python 3.10）
conda create -n wakefusion_vision python=3.10 -y

# 激活环境
conda activate wakefusion_vision

# 验证环境
python --version
# 应输出：Python 3.10.x
```

#### 步骤4：安装视觉环境依赖

```powershell
# 确保在 wakefusion_vision 环境中
conda activate wakefusion_vision

# 安装视觉处理依赖
pip install mediapipe opencv-python pyorbbecsdk numpy pyyaml pyzmq pydantic
```

**安装说明**：
- **预计耗时**：5-10 分钟
- **关键依赖**：
  - `mediapipe`（Google MediaPipe，用于人脸检测和唇动检测）
  - `opencv-python`（OpenCV，用于图像处理）
  - `pyorbbecsdk`（Orbbec Femto Bolt SDK，用于深度相机）
  - `numpy`（数值计算）
  - `pyyaml`、`pyzmq`、`pydantic`（与主环境相同的通信和配置库）

**常见问题**：
- **MediaPipe 安装慢**：使用国内镜像源
  ```powershell
  pip install mediapipe -i https://pypi.tuna.tsinghua.edu.cn/simple
  ```
- **pyorbbecsdk 安装失败**：
  - 确保已安装 Orbbec SDK 驱动
  - 从 Orbbec 官网下载对应版本的 SDK
  - 或使用预编译的 wheel 文件

### 1.3 环境验证

#### 验证主环境 `wakefusion`

```powershell
conda activate wakefusion

# 验证关键依赖
python -c "import torch; print(f'PyTorch: {torch.__version__}')"
python -c "import nemo; print('NeMo: OK')"
python -c "import zmq; print(f'ZMQ: {zmq.zmq_version()}')"
python -c "import websockets; print('WebSockets: OK')"

# 验证项目模块
python -c "import wakefusion; print('WakeFusion: OK')"
```

**预期输出**：
```
PyTorch: 2.0.0
NeMo: OK
ZMQ: 4.3.4
WebSockets: OK
WakeFusion: OK
```

#### 验证视觉环境 `wakefusion_vision`

```powershell
conda activate wakefusion_vision

# 验证关键依赖
python -c "import cv2; print(f'OpenCV: {cv2.__version__}')"
python -c "import mediapipe as mp; print('MediaPipe: OK')"
python -c "import numpy; print('NumPy: OK')"

# 验证项目模块
python -c "import wakefusion; print('WakeFusion: OK')"
```

**预期输出**：
```
OpenCV: 4.8.0
MediaPipe: OK
NumPy: OK
WakeFusion: OK
```

### 1.4 环境管理

#### 查看所有环境
```powershell
conda env list
```

#### 备份环境（推荐）
```powershell
# 备份主环境
conda env export -n wakefusion > wakefusion_backup.yml

# 备份视觉环境
conda env export -n wakefusion_vision > wakefusion_vision_backup.yml
```

#### 恢复环境
```powershell
# 从备份恢复
conda env create -f wakefusion_backup.yml
conda env create -f wakefusion_vision_backup.yml
```

#### 删除环境（谨慎操作）
```powershell
# 删除环境（需要先退出该环境）
conda deactivate
conda env remove -n wakefusion
conda env remove -n wakefusion_vision
```

---

## 二、启动流程

### 2.1 启动前检查

#### 检查清单
- [ ] 两个 conda 环境已创建并安装依赖
- [ ] 配置文件 `config/config.yaml` 已正确配置
- [ ] 硬件设备（麦克风/摄像头）已连接
- [ ] 端口未被占用（5555、5556、5557、8765、8080）
- [ ] 网络连接正常（首次运行需要下载 Silero VAD 模型）

#### 检查端口占用（Windows）
```powershell
# 检查端口是否被占用
netstat -ano | findstr "5555"
netstat -ano | findstr "5556"
netstat -ano | findstr "5557"
netstat -ano | findstr "8765"
netstat -ano | findstr "8080"

# 如果端口被占用，可以：
# 1. 关闭占用端口的程序
# 2. 修改 config/config.yaml 中的端口配置
```

### 2.2 一键启动（推荐）

#### 使用 PowerShell 启动脚本

```powershell
# 在项目根目录下执行
.\launcher.ps1
```

**操作步骤**：
1. 打开 **Anaconda PowerShell Prompt**
2. 切换到项目目录：
   ```powershell
   cd D:\tools\cursor_project\wakefusion_wake_module
   ```
3. 运行启动脚本：
   ```powershell
   .\launcher.ps1
   ```
4. 选择选项：
   - 输入 `1`：启动所有服务
   - 输入 `2`：停止所有服务

**启动过程**：
- 脚本会自动按顺序启动 3 个服务，每个服务在独立的 PowerShell 窗口中运行
- 启动顺序：Vision Service → Audio Service → Core Server
- 每个服务启动间隔 3 秒，确保前一个服务完全启动

**预期输出**：
```
============================================================
Starting all services...
============================================================

[1/3] Starting Vision Service (env: wakefusion_vision)...
[2/3] Starting Audio Service (env: wakefusion)...
[3/3] Starting Core Server (env: wakefusion)...

============================================================
All services started!
============================================================

Service List:
  - Vision Service (ZMQ Port: 5555)
  - Audio Service (ZMQ Port: 5556, 5557)
  - Core Server  (ZMQ Port: 5561, 5564, WebSocket Client → LLM Agent: 8080)
```

#### 验证服务启动

**Vision Service 窗口**应显示：
```
Vision Service started
ZMQ Publisher bound to: tcp://*:5555
Camera initialized: Orbbec Femto Bolt (或 USB Camera)
```

**Audio Service 窗口**应显示：
```
Audio Service started
ZMQ Publisher bound to: tcp://*:5556
ZMQ REP bound to: tcp://*:5557
Audio device: XVF3800 (或 Default Device)
KWS model loaded: matchboxnet
VAD model loaded: silero_vad
```

**Core Server 窗口**应显示：
```
Core Server started
ZMQ Subscriber connected to Vision: tcp://127.0.0.1:5555
ZMQ Subscriber connected to Audio: tcp://127.0.0.1:5556
ZMQ REP bound to: tcp://*:5561
WebSocket Server started: ws://127.0.0.1:8765
WebSocket Client connecting to: ws://127.0.0.1:8080
```

**如果看到错误**：
- 检查对应服务的窗口日志
- 参考 [故障排查](#七故障排查) 章节

### 2.3 手动启动（调试用）

手动启动适用于调试单个服务或排查问题。

#### 启动顺序

**重要**：必须按以下顺序启动，因为 Core Server 依赖 Vision 和 Audio 服务的数据流。

#### 步骤1：启动 Vision Service

```powershell
# 打开终端1（Anaconda PowerShell Prompt）
conda activate wakefusion_vision
cd D:\tools\cursor_project\wakefusion_wake_module
python -m wakefusion.services.vision_service --fps 15
```

**参数说明**：
- `--fps 15`：设置视觉处理帧率（默认 15 FPS，降低可减少 CPU 负载）

**验证**：
- 窗口应显示：`Vision Service started`
- 窗口应显示：`ZMQ Publisher bound to: tcp://*:5555`
- 如果使用 Orbbec 相机，应显示：`Camera initialized: Orbbec Femto Bolt`

**常见问题**：
- **找不到摄像头**：检查 USB 连接，或修改 `--camera` 参数指定摄像头索引
- **ZMQ 绑定失败**：检查端口 5555 是否被占用

#### 步骤2：启动 Audio Service

```powershell
# 打开终端2（Anaconda PowerShell Prompt）
conda activate wakefusion
cd D:\tools\cursor_project\wakefusion_wake_module
python -m wakefusion.services.audio_service
```

**验证**：
- 窗口应显示：`Audio Service started`
- 窗口应显示：`ZMQ Publisher bound to: tcp://*:5556`
- 窗口应显示：`ZMQ REP bound to: tcp://*:5557`
- 窗口应显示：`Audio device: XVF3800`（或你的设备名称）
- 窗口应显示：`KWS model loaded: matchboxnet`
- 窗口应显示：`VAD model loaded: silero_vad`（首次运行会下载模型，需要网络）

**常见问题**：
- **找不到音频设备**：检查 `config/config.yaml` 中的 `audio.device_match` 配置
- **KWS 模型加载失败**：检查模型文件是否存在（`xiaokang_xvf3800_pro.nemo` 或 `xiaokang_oww.onnx`）
- **Silero VAD 下载失败**：检查网络连接，或手动下载模型

#### 步骤3：启动 Core Server

```powershell
# 打开终端3（Anaconda PowerShell Prompt）
conda activate wakefusion
cd D:\tools\cursor_project\wakefusion_wake_module
python -m wakefusion.services.core_server
```

**验证**：
- 窗口应显示：`Core Server started`
- 窗口应显示：`ZMQ Subscriber connected to Vision: tcp://127.0.0.1:5555`
- 窗口应显示：`ZMQ Subscriber connected to Audio: tcp://127.0.0.1:5556`
- 窗口应显示：`WebSocket Server started: ws://127.0.0.1:8765`
- 窗口应显示：`WebSocket Client connecting to: ws://127.0.0.1:8080`
- 如果 LLM Agent 已启动，应显示：`✅ WebSocket已连接`

**常见问题**：
- **无法连接 Vision/Audio**：确保 Vision 和 Audio 服务已启动
- **WebSocket 连接失败**：确保 LLM Agent 服务端已启动（或使用 Mock LLM Agent）

### 2.4 启动 Web 页面（可选）

Web 页面用于可视化显示对话字幕和系统状态。

#### 首次运行

```powershell
# 打开终端4（普通 PowerShell 或 CMD）
cd D:\tools\cursor_project\wakefusion_wake_module\wakefusion_web

# 安装依赖（仅首次运行）
npm install

# 启动开发服务器
npm run dev
```

**预期输出**：
```
  VITE v6.2.0  ready in 500 ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: http://192.168.x.x:3000/
```

#### 访问 Web 页面

1. 打开浏览器，访问：`http://localhost:3000`
2. 页面应自动连接到 Core Server（`ws://127.0.0.1:8765`）
3. 如果连接成功，页面应显示连接状态

**验证**：
- 浏览器控制台（F12）应显示：`✅ Web UI 已成功连接到 Core Server`
- 页面应显示实时字幕（当有对话时）

### 2.5 启动 Mock LLM Agent（测试用）

当没有接入真实服务端时，使用 Mock LLM Agent 进行测试。

```powershell
# 打开终端5（Anaconda PowerShell Prompt）
conda activate wakefusion
cd D:\tools\cursor_project\wakefusion_wake_module
python tests/mock_llm_agent_simple.py
```

**预期输出**：
```
🚀 Mock LLM Agent 已启动!
  监听地址: ws://127.0.0.1:8080
  等待 Core Server 连接...
```

**功能**：
- 模拟 ASR 识别结果（自动识别"你好小康"）
- 模拟 AI 回复文本（自动回复"你好，有什么可以帮助你的吗？"）
- 模拟 TTS 音频流（发送提示音）
- 支持 interrupt/stop 指令（输入 `q` 发送 interrupt，输入 `s` 发送 stop）

**验证**：
- Core Server 窗口应显示：`✅ WebSocket已连接`
- Mock LLM Agent 窗口应显示：`🔔 Core Server 已连接`

### 2.6 完整启动流程示例

**推荐启动顺序**（用于完整测试）：

1. **启动 Vision Service**（终端1）
2. **启动 Audio Service**（终端2）
3. **启动 Core Server**（终端3）
4. **启动 Mock LLM Agent**（终端4，可选）
5. **启动 Web 页面**（终端5，可选）

**验证所有服务**：
- 所有服务窗口应显示正常启动日志
- Core Server 应成功连接到 Vision 和 Audio
- Core Server 应成功连接到 LLM Agent（Mock 或真实服务端）
- Web 页面应成功连接到 Core Server

---

## 三、后端模块说明

### 3.1 Vision Service（视觉服务）

**功能**：
- 人脸检测（使用 MediaPipe）
- 唇动检测（基于人脸关键点）
- 距离估计（使用 Orbbec 深度相机）
- 视觉唤醒判断（4米内 + 正面率 ≥ 70%）

**技术栈**：
- MediaPipe（Google，人脸检测）
- OpenCV（图像处理）
- Orbbec SDK（深度相机）

**输出**：
- 通过 ZMQ PUB 发布视觉数据（端口 5555）
- 数据格式：JSON，包含 `wake`、`distance`、`face_count`、`is_talking` 等字段

**环境**：`wakefusion_vision`

**启动命令**：
```powershell
conda activate wakefusion_vision
python -m wakefusion.services.vision_service --fps 15
```

### 3.2 Audio Service（音频服务）

**功能**：
- 音频采集（16kHz，20ms 帧）
- VAD（Voice Activity Detection，使用 Silero VAD）
- KWS（Keyword Spotting，使用 MatchboxNet 或 OpenWakeWord）
- 音频播放（TTS 音频流播放）

**技术栈**：
- PyAudio（音频采集）
- Silero VAD（语音活动检测）
- NeMo MatchboxNet（唤醒词检测）
- SoundDevice（音频播放）

**通信**：
- ZMQ PUB（端口 5556）：发布音频数据和事件
- ZMQ REP（端口 5557）：接收控制指令（阈值调整、停止播放等）

**环境**：`wakefusion`

**启动命令**：
```powershell
conda activate wakefusion
python -m wakefusion.services.audio_service
```

### 3.3 Core Server（核心服务）

**功能**：
- 多模态融合（音频 + 视觉）
- 状态机管理（IDLE → VISUAL_WAKE → LISTENING → SPEAKING）
- WebSocket 客户端（连接 LLM Agent）
- WebSocket 服务端（转发字幕给 Web UI）

**职责**：
- 订阅 Vision/Audio 数据流（ZMQ SUB）
- 决策唤醒/截断逻辑（基于多模态数据）
- 连接 LLM Agent（WebSocket 客户端，端口 8080）
- 转发字幕给 Web UI（WebSocket 服务端，端口 8765）

**环境**：`wakefusion`

**启动命令**：
```powershell
conda activate wakefusion
python -m wakefusion.services.core_server
```

---

## 四、通信机制

### 4.1 后端内部通信（ZMQ）

| 模块 | 端口 | 协议 | 方向 | 说明 |
|------|------|------|------|------|
| Vision → Core | 5555 | ZMQ PUB-SUB | 视觉数据流 | 允许丢帧，高频数据 |
| Audio → Core | 5556 | ZMQ PUB-SUB | 音频数据流 | 允许丢帧，高频数据 |
| Core → Audio | 5557 | ZMQ REQ-REP | 控制指令 | 必须可靠，低频控制 |
| Core → Vision | 5564 | ZMQ PUB-SUB | 控制指令 | 允许丢帧，低频控制 |

**特点**：
- **PUB-SUB**：一对多，允许丢帧，适合高频数据流
- **REQ-REP**：一对一，必须可靠，适合低频控制指令

### 4.2 后端与 Web UI 通信（WebSocket）

- **连接方式**：Core Server 作为 WebSocket 服务端
- **地址**：`ws://127.0.0.1:8765`
- **协议**：WebSocket（JSON 消息）

**消息类型**：
- `subtitle_user`：用户 ASR 字幕
- `subtitle_ai`：AI 回复字幕
- `subtitle_clear`：清空字幕
- `playAudio`：Unity 音频播放指令

### 4.3 客户端与服务端通信（WebSocket）

- **连接方式**：Core Server 作为 WebSocket 客户端
- **地址**：`ws://127.0.0.1:8080/api/voice/ws?deviceId=xxx&token=xxx`
- **配置**：`config/config.yaml` → `llm_agent.host`

**消息类型**：

**上行（Core Server → LLM Agent）**：
- `audio_start`：开始音频流
- `audio_end`：结束音频流
- `audio_cancel`：取消音频流
- 音频二进制流：16kHz PCM 音频数据

**下行（LLM Agent → Core Server）**：
- `asr`：ASR 识别结果（partial 或 final）
- `text`：AI 回复文本
- `route`：路由消息（触发 TTS）
- TTS 音频流：二进制 PCM 音频数据
- `interrupt`：打断指令
- `stop`：停止指令

---

## 五、核心机制

### 5.1 多模态唤醒

**纯语音唤醒**：
- 条件：KWS 置信度 ≥ 0.9
- 场景：用户距离较远或不在摄像头视野内

**视觉降维打击**：
- 条件：4米内 + 正面率 ≥ 40%
- 效果：音频阈值从 0.9 降至 0.6
- 场景：用户在摄像头前，更容易唤醒

### 5.2 状态机

**状态流转**：
```
IDLE → VISUAL_WAKE → LISTENING → SPEAKING → LISTENING → IDLE
```

**状态说明**：
- **IDLE**：待机状态，等待唤醒
- **VISUAL_WAKE**：视觉唤醒，降低音频阈值
- **LISTENING**：监听状态，等待用户说话
- **SPEAKING**：用户正在说话，发送音频流给 LLM Agent

**超时机制**：
- **90秒全局超时**：无交互则退出交互模式
- **5秒宏观发呆**：无 trace_id 时，5秒静默则退出
- **1.5秒微观截断**：有 trace_id 时，1.5秒静默则截断

### 5.3 防抖机制

**视觉进入防抖**：200ms
- 防止快速进出摄像头视野导致的误判

**视觉离开防抖**：1000ms
- 防止短暂离开摄像头视野导致的误判

**物理防抖护盾**：TTS 结束后 0.8 秒内忽略音频
- 防止 TTS 播放时的回声触发误唤醒

### 5.4 截断逻辑

**微观截断**：
- 条件：有 trace_id（正在处理对话）
- 阈值：1.5秒静默
- 效果：截断当前对话，等待下一轮

**宏观发呆**：
- 条件：无 trace_id（未在处理对话）
- 阈值：5秒静默
- 效果：退出交互模式，返回 IDLE

**连续6次发呆**：
- 效果：自动退出交互模式，防止误唤醒循环

---

## 六、配置说明

### 6.1 主配置文件

**位置**：`config/config.yaml`

**关键配置项**：

#### 音频配置
```yaml
audio:
  device_match: "default"  # 设备匹配: "XVF3800" 或 "default"
  capture_sample_rate: 48000  # 采集采样率
  work_sample_rate: 16000  # 工作采样率
```

#### KWS 配置
```yaml
kws:
  enabled: true
  engine: "matchboxnet"  # KWS引擎: matchboxnet 或 openwakeword
  threshold: 0.5  # 检测阈值
```

#### VAD 配置
```yaml
vad:
  enabled: true
  engine: "silero"  # VAD引擎
  threshold: 0.5  # 语音概率阈值（0.0-1.0）
```

#### LLM Agent 配置
```yaml
llm_agent:
  host: "127.0.0.1:8080"  # LLM Agent服务地址
  device_id: "wakefusion-device-01"  # 设备标识
  token: "your-token-here"  # 认证令牌
```

#### 视觉唤醒配置
```yaml
vision_wake:
  detection_distance_m: 4.0  # 4米内开始检测
  frontal_percent_threshold: 70  # 正面率阈值（%）
```

#### 音频动态阈值配置
```yaml
audio_threshold:
  default: 0.9  # 默认高阈值（无人时）
  visual_wake: 0.6  # 视觉唤醒后的低阈值
```

### 6.2 修改配置

1. 编辑 `config/config.yaml`
2. 保存文件
3. **重启对应服务**（修改配置后必须重启）

**注意**：
- 修改音频配置 → 重启 Audio Service
- 修改视觉配置 → 重启 Vision Service
- 修改 LLM Agent 配置 → 重启 Core Server
- 修改其他配置 → 重启 Core Server

---

## 七、故障排查

### 7.1 环境问题

#### 问题：Conda 环境创建失败
**解决方案**：
```powershell
# 检查 conda 是否可用
conda --version

# 更新 conda
conda update conda

# 清理 conda 缓存
conda clean --all
```

#### 问题：依赖安装失败
**解决方案**：
- 使用国内镜像源：
  ```powershell
  pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
  ```
- 逐个安装依赖，定位问题包
- 检查 Python 版本（必须是 3.10 或 3.11）

#### 问题：环境混用导致错误
**解决方案**：
- 确保 Vision Service 在 `wakefusion_vision` 环境运行
- 确保 Audio Service 和 Core Server 在 `wakefusion` 环境运行
- 使用 `conda activate` 切换环境

### 7.2 启动问题

#### 问题：端口被占用
**解决方案**：
```powershell
# 查找占用端口的进程
netstat -ano | findstr "5555"

# 结束进程（替换 PID 为实际进程ID）
taskkill /PID <PID> /F

# 或修改 config/config.yaml 中的端口配置
```

#### 问题：Vision Service 无法启动摄像头
**解决方案**：
- 检查 USB 连接
- 检查摄像头驱动
- 修改启动参数：`python -m wakefusion.services.vision_service --camera 0`
- 检查 `config/config.yaml` 中的视觉配置

#### 问题：Audio Service 无法找到音频设备
**解决方案**：
- 检查 `config/config.yaml` 中的 `audio.device_match` 配置
- 运行测试脚本：`python -m tests.list_audio_devices`
- 检查音频设备是否被其他程序占用

#### 问题：Core Server 无法连接 Vision/Audio
**解决方案**：
- 确保 Vision 和 Audio 服务已启动
- 检查 ZMQ 端口配置（5555、5556）
- 检查防火墙设置

#### 问题：Core Server 无法连接 LLM Agent
**解决方案**：
- 确保 LLM Agent 服务端已启动（或使用 Mock LLM Agent）
- 检查 `config/config.yaml` 中的 `llm_agent.host` 配置
- 检查网络连接
- 检查防火墙设置

### 7.3 运行时问题

#### 问题：Silero VAD 模型下载失败
**解决方案**：
- 检查网络连接
- 首次运行需要联网下载模型
- 模型会缓存到本地，后续运行无需网络

#### 问题：KWS 模型加载失败
**解决方案**：
- 检查模型文件是否存在（`xiaokang_xvf3800_pro.nemo` 或 `xiaokang_oww.onnx`）
- 检查模型文件路径配置
- 检查文件权限

#### 问题：Web 页面无法连接 Core Server
**解决方案**：
- 确保 Core Server 已启动
- 检查 WebSocket 端口（8765）是否被占用
- 检查浏览器控制台错误信息
- 检查防火墙设置

### 7.4 性能问题

#### 问题：CPU 占用过高
**解决方案**：
- 降低视觉处理帧率：`--fps 10`（默认 15）
- 关闭不必要的功能
- 检查是否有其他程序占用 CPU

#### 问题：内存占用过高
**解决方案**：
- 检查是否有内存泄漏
- 重启服务
- 检查日志文件大小（定期清理）

### 7.5 日志调试

#### 启用 DEBUG 日志
```yaml
# config/config.yaml
runtime:
  log_level: "DEBUG"
```

#### 查看日志文件
- 日志文件位置：`logs/` 目录
- 日志格式：按日期和模块分类

#### 常见日志信息
- `✅`：成功操作
- `❌`：错误操作
- `⚠️`：警告信息
- `🔔`：连接事件
- `📨`：消息收发

---

## 八、快速检查清单

### 首次使用
- [ ] Conda 已安装并配置环境变量
- [ ] Python 3.10 或 3.11 已安装
- [ ] 两个 conda 环境已创建（`wakefusion`、`wakefusion_vision`）
- [ ] 所有依赖已安装
- [ ] 环境验证通过
- [ ] 硬件设备已连接（麦克风/摄像头）
- [ ] 配置文件 `config/config.yaml` 已正确配置

### 每次启动
- [ ] 端口未被占用
- [ ] 启动顺序正确（Vision → Audio → Core）
- [ ] 所有服务窗口显示正常启动日志
- [ ] Core Server 成功连接到 Vision 和 Audio
- [ ] Core Server 成功连接到 LLM Agent（Mock 或真实服务端）
- [ ] Web 页面已启动（可选）
- [ ] Web 页面成功连接到 Core Server（可选）

### 测试验证
- [ ] 视觉唤醒测试通过（走到摄像头前，应触发视觉唤醒）
- [ ] 音频唤醒测试通过（说唤醒词，应触发音频唤醒）
- [ ] 对话测试通过（说"你好小康"，应收到 AI 回复）
- [ ] 截断测试通过（静默 1.5 秒，应自动截断）

---

## 附录

### A. 常用命令速查

```powershell
# 环境管理
conda activate wakefusion
conda activate wakefusion_vision
conda deactivate
conda env list

# 服务启动
python -m wakefusion.services.vision_service --fps 15
python -m wakefusion.services.audio_service
python -m wakefusion.services.core_server

# 测试工具
python -m tests.list_audio_devices
python tests/mock_llm_agent_simple.py

# Web 页面
cd wakefusion_web
npm run dev
```

### B. 端口列表

| 端口 | 服务 | 说明 |
|------|------|------|
| 5555 | Vision Service | ZMQ PUB（视觉数据流） |
| 5556 | Audio Service | ZMQ PUB（音频数据流） |
| 5557 | Audio Service | ZMQ REP（控制指令） |
| 5561 | Core Server | ZMQ REP（LLM 控制） |
| 5564 | Core Server | ZMQ PUB（视觉控制） |
| 8765 | Core Server | WebSocket Server（Web UI） |
| 8080 | LLM Agent | WebSocket Server（服务端） |
| 3000 | Web 页面 | HTTP Server（开发模式） |

### C. 文件结构

```
wakefusion_wake_module/
├── config/
│   └── config.yaml          # 主配置文件
├── wakefusion/
│   ├── services/
│   │   ├── vision_service.py
│   │   ├── audio_service.py
│   │   └── core_server.py
│   └── workers/
│       └── lip_sync_detector.py
├── wakefusion_web/          # Web 页面
├── tests/                   # 测试脚本
├── launcher.ps1             # 一键启动脚本
└── requirements.txt         # 依赖列表
```

---

**文档版本**：v1.0  
**最后更新**：2026-03-18  
**维护者**：WakeFusion Team
