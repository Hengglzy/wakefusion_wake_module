# WakeFusion 链路说明手册

## 目录

1. [系统架构概述](#系统架构概述)
2. [系统状态机](#系统状态机)
3. [核心机制详解](#核心机制详解)
4. [数据流链路](#数据流链路)
5. [典型场景实例](#典型场景实例)
6. [配置参数说明](#配置参数说明)

---

## 系统架构概述

WakeFusion是一个多模态唤醒和交互系统，整合音频、视觉、ASR、TTS等模块，实现智能数字人对话。

### 模块组成

- **音频模块** (audio_service.py): 麦克风采集、KWS唤醒词检测、VAD语音活动检测
- **视觉模块** (vision_service.py): 摄像头采集、人脸检测、距离估算、唇动检测
- **核心服务器** (core_server.py): 多模态融合决策、状态管理、音频流控制
- **ASR模块** (asr_service.py): 语音识别、流式识别、WebSocket输出
- **TTS模块** (tts_service.py): 语音合成、流式输出、音频播放

### 通信协议

- **ZMQ PUB-SUB**: 数据流传输（视觉数据、音频数据）
- **ZMQ REQ-REP**: 控制指令（阈值调整、流控制）
- **ZMQ PUSH-PULL**: 音频数据传输（Core→ASR, TTS→Core）
- **WebSocket**: LLM通信（ASR→LLM, LLM→TTS）

---

## 系统状态机

### 状态定义

系统包含5个核心状态：

1. **IDLE**: 空闲状态，等待唤醒
2. **VISUAL_WAKE**: 视觉唤醒状态，已检测到用户但未说话
3. **LISTENING**: 监听状态，正在接收用户语音
4. **PROCESSING**: 处理状态，ASR识别完成，等待LLM回复
5. **SPEAKING**: 播报状态，数字人正在说话

### 状态转换图

```
IDLE
  ├─[视觉唤醒]→ VISUAL_WAKE
  │              ├─[用户说话]→ LISTENING
  │              └─[用户离开]→ IDLE
  │
  ├─[纯语音唤醒]→ LISTENING
  │
LISTENING
  ├─[VAD超时]→ PROCESSING
  ├─[视觉斩断]→ IDLE
  └─[30秒保底]→ PROCESSING
  │
PROCESSING
  ├─[收到TTS音频]→ SPEAKING
  └─[唤醒词]→ LISTENING (持续对话)
  │
SPEAKING
  ├─[播放完成]→ LISTENING (持续对话)
  ├─[硬打断]→ LISTENING
  └─[视觉斩断]→ IDLE (持续对话期)
```

---

## 核心机制详解

### 1. 视觉唤醒机制

**触发条件**:
- 检测到人脸距离 ≤ 3.0米
- 人脸正面率 ≥ 75%
- 同时满足以上两个条件

**状态维护**:
- 未唤醒: 持续检查是否满足唤醒条件
- 已唤醒: 检查所有人脸是否在[0.1m, 3.5m]区间内
- 离开判定: 连续20帧（约1.5秒）所有人脸都不在有效区间，则取消唤醒

**实例**:
- 用户走到摄像头前2.5米，正面看摄像头（正面率80%）
- 预期: 视觉唤醒状态变为True，系统进入VISUAL_WAKE状态
- 用户后退到4米外，持续1.5秒
- 预期: 视觉唤醒状态变为False，系统返回IDLE状态

### 2. 音频唤醒机制

系统支持两种唤醒路径：

#### 路径A: 视觉降维打击

**触发条件**:
- 当前状态: VISUAL_WAKE
- 唤醒词置信度 ≥ 0.4（visual_wake阈值）

**流程**:
1. 视觉模块检测到用户（距离≤3m，正面率≥75%）
2. Core Server进入VISUAL_WAKE状态，发送降阈指令（0.95→0.4）
3. 用户说唤醒词"你好小康"
4. 音频模块检测到唤醒词，置信度≥0.4
5. Core Server进入LISTENING状态

**实例**:
- 用户在摄像头前说"你好小康"（置信度60%）
- 预期: 系统进入LISTENING状态，开始接收音频

#### 路径B: 纯语音唤醒

**触发条件**:
- 当前状态: IDLE
- 唤醒词置信度 ≥ 0.95（default阈值）

**流程**:
1. 用户不在摄像头前（或距离>3m）
2. 用户说唤醒词"你好小康"，置信度≥0.95
3. Core Server直接进入LISTENING状态

**实例**:
- 用户在摄像头外说"你好小康"（置信度96%）
- 预期: 系统进入LISTENING状态，开始接收音频

### 3. 硬打断机制

**触发条件**:
- 当前状态: SPEAKING（数字人正在说话）
- 检测到唤醒词，置信度达到阈值：
  - 视觉唤醒路径: ≥ 0.4
  - 纯语音唤醒路径: ≥ 0.95

**处理流程**:
1. 清空TTS播放队列
2. 发送STOP_SYNTHESIS信号给TTS模块
3. 启动1.5秒冷却期（忽略VAD和KWS事件）
4. 进入LISTENING状态

**实例**:
- 数字人正在播放"今天天气很好..."
- 用户说"你好小康"（置信度65%，视觉唤醒路径）
- 预期: TTS立即停止，播放队列清空，系统进入LISTENING状态

### 4. 视觉斩断机制

视觉斩断是最高优先级的打断机制，当检测到用户离开摄像头时立即切断交互。

**触发条件**:
- vision_wake == False（视觉唤醒状态机判定用户已离开）
- visual_cutoff_enabled == True

**场景A: LISTENING状态下的视觉斩断（关麦）**

触发条件:
- 当前状态: LISTENING
- vision_wake == False
- 排除: 纯语音唤醒的首次对话（conversation_round==0 且 wake_path=="audio"）

处理:
- 发送ABORT_SPEECH标记给ASR
- 停止音频推流
- 状态回落到IDLE

**场景B: SPEAKING状态下的视觉斩断（停止TTS）**

触发条件:
- 当前状态: SPEAKING
- vision_wake == False
- conversation_round >= 1（持续对话期）

处理:
- 发送STOP_SYNTHESIS信号给TTS
- 状态回落到IDLE

**实例**:
- 用户在LISTENING状态下说话，然后离开摄像头
- 预期: 系统立即停止音频推流，发送ABORT_SPEECH，返回IDLE状态

### 5. VAD超时机制

VAD超时机制采用宏微观双重监控，确保在用户说完话后及时截断。

#### 宏观超时（发呆检测）

**触发条件**:
- 用户未开口（_user_has_spoken == False）
- 距离上次VAD检测时间 > 2.0秒（vad_silence_timeout_default_sec）

**处理**:
- 如果vision_wake == True: 进入VISUAL_WAKE状态
- 如果vision_wake == False: 进入IDLE状态
- 发送ABORT_SPEECH（如果用户未开口）或END_OF_SPEECH

**实例**:
- 用户唤醒后2秒内没有说话
- 预期: 系统返回VISUAL_WAKE或IDLE状态

#### 微观超时（说话结束检测）

**触发条件**:
- 用户已开口（_user_has_spoken == True）
- 满足以下任一条件：

**策略1: 双模态快刀**
- time_since_last_vad > 1.5秒 且 time_since_lip_closed > 1.5秒

**策略2: 视觉强杀**
- time_since_lip_closed > 2.0秒（无视音频VAD）

**策略3: 纯音频兜底**
- time_since_last_vad > 2.5秒（用户没看屏幕时）

**处理**:
- 发送END_OF_SPEECH标记给ASR
- 停止音频推流
- 进入PROCESSING状态

**实例**:
- 用户说完话，音频VAD静音1.0秒，视觉检测嘴巴闭合1.0秒
- 预期: 系统触发双模态快刀，进入PROCESSING状态

### 6. 30秒保底机制

**触发条件**:
- 当前状态: LISTENING
- 进入LISTENING状态后已持续30秒

**处理**:
- 强制发送END_OF_SPEECH标记
- 停止音频推流
- 进入PROCESSING状态

**目的**: 防止在视觉受损或极度噪音环境下，其他截断机制失效导致数据无限制传输

**实例**:
- 用户在极度噪音环境下说话，VAD和视觉检测都失效
- 预期: 30秒后系统强制截断，进入PROCESSING状态

### 7. 持续对话机制

**触发条件**:
- TTS播放完成，自动从SPEAKING转换到LISTENING
- 或PROCESSING状态下检测到唤醒词（置信度≥0.4）

**流程**:
1. conversation_round递增（首次=0，持续对话≥1）
2. 发送start_streaming命令给音频模块
3. 进入LISTENING状态，等待用户继续说话

**实例**:
- 数字人回答完成，系统自动进入LISTENING状态
- 用户在2秒内继续说话
- 预期: 系统继续接收音频，无需再次唤醒

---

## 数据流链路

### 完整对话链路

```
用户说话
  ↓
音频模块 (KWS + VAD)
  ↓ ZMQ PUB (Multipart: JSON + PCM)
Core Server
  ↓ ZMQ PUSH (PCM音频)
ASR模块 (FunASR流式识别)
  ↓ WebSocket (JSON文本)
LLM模块
  ↓ WebSocket (JSON文本)
TTS模块 (Qwen3-TTS合成)
  ↓ ZMQ PUSH (Multipart: JSON + PCM)
Core Server
  ↓ 音频播放队列
sounddevice → XVF3800扬声器
```

### 视觉数据链路

```
摄像头
  ↓
视觉模块 (MediaPipe)
  ↓ ZMQ PUB (JSON)
Core Server
  ↓
视觉唤醒状态机
  ↓
视觉斩断判断
```

### 控制指令链路

```
Core Server
  ↓ ZMQ REQ-REP
音频模块
  - set_threshold: 动态调整唤醒词阈值
  - start_streaming: 开启音频推流
  - stop_streaming: 停止音频推流
  - reset_cooldown: 重置冷却期

Core Server
  ↓ ZMQ PUB
TTS模块
  - STOP_SYNTHESIS: 停止合成
  - ABORT_ASR: 带外强杀信号（通过tts_stop_pub_port）
```

---

## 典型场景实例

### 场景1: 完整对话流程（视觉唤醒路径）

**步骤**:
1. 用户走到摄像头前（距离2.5m，正面率80%）
2. 系统进入VISUAL_WAKE状态，阈值降至0.4
3. 用户说"你好小康"（置信度60%）
4. 系统进入LISTENING状态，开始接收音频
5. 用户说"今天天气怎么样"
6. 用户说完，音频VAD静音1.0秒，视觉检测嘴巴闭合1.0秒
7. 系统进入PROCESSING状态，发送END_OF_SPEECH
8. ASR识别完成，发送文本给LLM
9. LLM生成回复，发送给TTS
10. TTS合成音频，发送给Core Server
11. Core Server进入SPEAKING状态，播放音频
12. 播放完成，系统进入LISTENING状态（持续对话）

**预期结果**:
- 状态转换: IDLE → VISUAL_WAKE → LISTENING → PROCESSING → SPEAKING → LISTENING
- 唤醒路径: visual
- 对话轮次: 0 → 1

### 场景2: 硬打断测试

**步骤**:
1. 系统处于SPEAKING状态，正在播放"今天天气很好，适合出门散步..."
2. 用户在播放过程中说"你好小康"（置信度65%，视觉唤醒路径）
3. 系统检测到唤醒词，触发硬打断

**预期结果**:
- TTS立即停止合成
- 播放队列被清空
- 系统进入LISTENING状态
- 启动1.5秒冷却期
- 日志显示: "硬打断：停止播报，清空队列，发送停止信号"

### 场景3: 视觉斩断测试

**步骤**:
1. 系统处于LISTENING状态（视觉唤醒路径，conversation_round=1）
2. 用户正在说话
3. 用户离开摄像头（距离>3.5m，持续1.5秒）
4. 视觉唤醒状态变为False

**预期结果**:
- 系统立即停止音频推流
- 发送ABORT_SPEECH标记给ASR
- 状态回落到IDLE
- 日志显示: "视觉斩断：检测到用户离开，立即切断监听"

### 场景4: 纯语音唤醒首次对话

**步骤**:
1. 用户不在摄像头前（或距离>3m）
2. 用户说"你好小康"（置信度96%）
3. 系统进入LISTENING状态
4. 用户说话，然后离开摄像头

**预期结果**:
- 唤醒路径: audio
- 首次对话（conversation_round=0）不触发视觉斩断
- 允许完成首次对话，即使用户离开摄像头

### 场景5: 宏观发呆超时

**步骤**:
1. 系统进入LISTENING状态
2. 用户唤醒后2秒内没有说话（_user_has_spoken == False）
3. 系统检测到宏观超时

**预期结果**:
- 如果vision_wake == True: 进入VISUAL_WAKE状态
- 如果vision_wake == False: 进入IDLE状态
- 发送ABORT_SPEECH标记（用户未开口）
- 日志显示: "检测到幽灵唤醒（用户未开口），强制丢弃音频"

### 场景6: 30秒保底机制

**步骤**:
1. 系统进入LISTENING状态
2. 在极度噪音环境下，VAD和视觉检测都失效
3. 系统持续接收音频30秒

**预期结果**:
- 30秒后系统强制截断
- 发送END_OF_SPEECH标记
- 进入PROCESSING状态
- 日志显示: "30秒交流上限时间已到，强制截断"

---

## 配置参数说明

### 视觉唤醒配置

```yaml
vision_wake:
  detection_distance_m: 3.0        # 唤醒检测距离（米）
  frontal_percent_threshold: 75    # 正面率阈值（%）
  distance_range: [0.1, 3.5]       # 保持唤醒的有效距离区间（米）
  leave_timeout_sec: 1.5           # 离开判定超时（秒）
  leave_check_frames: 20          # 连续N帧不在区间内才判定离开
  visual_cutoff_enabled: true     # 启用视觉斩断机制
```

### 音频阈值配置

```yaml
audio_threshold:
  default: 0.85      # 默认高阈值（无人时，纯语音唤醒）
  visual_wake: 0.6   # 视觉唤醒后的低阈值（视觉降维打击）
```

### 持续对话配置

```yaml
conversation:
  vad_silence_timeout_default_sec: 2.0  # 宏观发呆超时（秒）
  vad_check_interval_ms: 200            # VAD检查间隔（毫秒）
```

### VAD配置

```yaml
vad:
  engine: "silero"           # VAD引擎
  model_version: "v4"        # Silero VAD版本
  threshold: 0.5             # 语音概率阈值
  sample_rate: 16000        # 采样率
```

### 截断策略阈值

- **双模态快刀**: 音频静音1.0秒 且 嘴巴闭合1.0秒
- **视觉强杀**: 嘴巴闭合1.2秒（无视音频VAD）
- **纯音频兜底**: 音频静音1.5秒
- **宏观发呆**: 无语音2.0秒（用户未开口）
- **30秒保底**: 持续监听30秒

---

## 关键设计要点

### 1. 唤醒路径记录

系统在首次唤醒时记录唤醒路径（visual或audio），用于：
- 硬打断时选择正确的阈值（visual: 0.4, audio: 0.95）
- 视觉斩断时判断是否保护首次对话

### 2. 对话轮次管理

- conversation_round = 0: 首次唤醒
- conversation_round >= 1: 持续对话期

用于：
- 视觉斩断时判断是否保护首次回答
- 持续对话期用户离开立即停止TTS

### 3. 用户开口标记

_user_has_spoken标记用于区分：
- 幽灵唤醒（用户未开口）: 发送ABORT_SPEECH
- 正常唤醒（用户已开口）: 发送END_OF_SPEECH

可通过以下方式设置：
- 音频VAD检测到连续2次人声
- 视觉检测到is_talking=True

### 4. 音频流控制

- start_streaming: 开启音频推流（持续对话时）
- stop_streaming: 停止音频推流（截断时）
- 在SPEAKING状态下忽略所有音频输入（除唤醒词）

### 5. ASR拒收模式

- discarding = False: 正常接收音频
- discarding = True: 丢弃所有音频（收到ABORT_ASR信号后）
- START_OF_SPEECH: 解除拒收模式

### 6. 冷却期机制

硬打断后启动1.5秒冷却期，期间忽略VAD和KWS事件，防止误触发。

---

## 故障排查要点

### 问题1: 唤醒词检测不到

- 检查音频模块是否正常运行
- 检查阈值配置（default: 0.95, visual_wake: 0.4）
- 检查唤醒路径是否正确（visual路径使用0.4阈值）

### 问题2: 视觉斩断不生效

- 检查visual_cutoff_enabled是否为true
- 检查视觉唤醒状态机是否正确维护vision_wake
- 检查是否在保护场景（纯语音唤醒首次对话、首次回答）

### 问题3: VAD超时不触发

- 检查_user_has_spoken是否正确设置
- 检查VAD检查线程是否正常运行
- 检查时间戳是否正确更新

### 问题4: 硬打断不生效

- 检查唤醒词置信度是否达到阈值
- 检查唤醒路径（visual: 0.4, audio: 0.95）
- 检查TTS模块是否收到STOP_SYNTHESIS信号

### 问题5: ASR识别出"嗯"

- 检查是否在非LISTENING状态下发送了END_OF_SPEECH
- 检查是否在用户未开口时发送了END_OF_SPEECH（应发送ABORT_SPEECH）

---

## 总结

WakeFusion系统通过多模态融合、状态机管理、智能截断等机制，实现了稳定可靠的数字人交互系统。核心设计包括：

1. **双重唤醒路径**: 视觉降维打击（低阈值）和纯语音唤醒（高阈值）
2. **智能截断机制**: 宏微观双重监控、视觉强杀、30秒保底
3. **硬打断机制**: 支持在数字人说话时立即打断
4. **视觉斩断机制**: 最高优先级打断，用户离开立即切断
5. **持续对话机制**: 免唤醒窗口，提升交互流畅度

系统通过ZMQ和WebSocket实现模块间通信，确保低延迟和高可靠性。
